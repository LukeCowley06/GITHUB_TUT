# This script defines a function to calculate the area of a rectangle.
def calculate_area(length, width):
    area = length * width  # Fixed typo: 'lenght' -> 'length'
    return area

# Testing
output = calculate_area(5, 10)
print("The calculated area is:", output)  # Fixed syntax error: added a comma